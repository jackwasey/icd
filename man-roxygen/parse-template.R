#' @param condense Deprecated. The map is not condensed by default. If required,
#'   it can reduced to a smaller set of codes using \code{icd_condense}.
#' @param save_pkg_data logical whether to save the result in the source tree.
#'   Defaults to \code{FALSE}.
#' @return invisibly returns the list of vectors, where a co-morbidity name is
#'   associated with a character vector of ICD-9 codes.
